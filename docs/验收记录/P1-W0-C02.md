# P1-W0-C02 接口契约验收记录（日志补充）

| 项目 | 结论 |
|------|------|
| 契约编号 | P1-W0-C02 |
| 当前状态 | ✅ 已完成 |
| 日期 | 2026-07-23 |
| 前置 | PG-05/06/07 ✅ |
| 契约文件 | `docs/接口契约/P1-W0-C02.md` |

## 本次补充范围

| 模块 | 接口 | 结果 |
|------|------|------|
| 拉取日志列表 | `GET /api/v1/mail-fetch-logs` | ✅ 含筛选、分页、今日汇总 |
| 拉取日志详情 | `GET /api/v1/mail-fetch-logs/{id}` | ✅ 含错误原文与邮件样例 Message-ID |
| 拉取失败重试 | `POST /api/v1/mail-fetch-logs/{id}/retry` | ✅ 新写 MANUAL 拉取日志 |
| 发送日志列表 | `GET /api/v1/mail-send-logs` | ✅ 含类型/状态/工单/邮箱筛选与汇总 |
| 发送日志详情 | `GET /api/v1/mail-send-logs/{id}` | ✅ 含完整 SMTP 错误 |
| 发送手动重试 | `POST /api/v1/mail-send-logs/{id}/retry` | ✅ 不回滚工单；限制最大重试 |

## 与原型核对

| 原型 | 核对项 | 结果 |
|------|--------|------|
| PG-06 | 邮箱、结果、是否建单、时间筛选；详情；失败重试 | ✅ |
| PG-07 | 类型、状态、工单、邮箱、时间筛选；失败原因；手动重试；工单跳转 | ✅ |

## 自检结果

```json
{
  "mailboxSectionPreserved": "passed",
  "fetchLogApis": "passed",
  "sendLogApis": "passed",
  "enumsFrozen": "passed",
  "adminOnly": "passed",
  "noExport": "passed",
  "noTicketRollback": "passed"
}
```

## 下一步

契约已冻结。进入 `P1-W2-BE-04` IMAP 拉信（写 `mt_mail_fetch_log`）。
