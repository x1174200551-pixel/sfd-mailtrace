# P1-W0-C04-01 分配规则页面接口契约验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-27 11:30 CST |
| 验收范围 | 分配规则页面接口、字段、状态、权限契约 |
| 当前结论 | 分配规则前端实施准入通过 |

## 1. 契约文件

| 项目 | 路径 |
|------|------|
| 接口契约 | `docs/接口契约/P1-W0-C04.md` |
| 对应原型 | `docs/原型设计/11、邮件工单系统-分配规则原型.html/png` |
| 对应 Gate | PG-08 ✅ |

## 2. 覆盖接口

| 接口 | 用途 |
|------|------|
| `GET /api/v1/assignment-rules` | 规则列表、筛选、摘要统计 |
| `POST /api/v1/assignment-rules` | 新建规则 |
| `PUT /api/v1/assignment-rules/{id}` | 编辑规则 |
| `PATCH /api/v1/assignment-rules/{id}/enabled` | 启用或停用规则 |
| `PUT /api/v1/assignment-rules/sort` | 调整优先级排序 |
| `POST /api/v1/assignment-rules/test-match` | 测试匹配 |
| `DELETE /api/v1/assignment-rules/{id}` | 删除规则 |

## 3. 自检证据

| 命令/动作 | 结果 |
|------|------|
| 读取 `AssignmentRuleController` | 通过，路由与契约一致 |
| 读取 `interfaces/vo/assignment/*.java` | 通过，请求/响应字段与契约一致 |
| 契约静态核对 | 通过，覆盖原型所需列表、编辑、启停、排序、测试匹配、删除和四态 |

## 4. 后续

下一步进入 `P1-W5-FE-01 分配规则` 前端实施。
