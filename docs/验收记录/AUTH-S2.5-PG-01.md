# AUTH-S2.5-PG-01 角色配置管理原型验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-28 17:01 CST |
| 当前状态 | 🧪 已输出，待谭总确认 |
| 原型文件 | `docs/原型设计/16、邮件工单系统-角色配置管理原型.html` |
| 截图文件 | `docs/原型设计/16、邮件工单系统-角色配置管理原型.png` |

## 1. 覆盖范围

1. 系统管理下新增“角色管理”独立页面，用户分配入口仍保留在用户管理。
2. 覆盖角色列表、新建/编辑角色、内置角色保护、自定义角色启停和复制。
3. 覆盖菜单权限、操作权限、默认数据范围和保存前生效预览。
4. 当前阶段不引入指定邮箱、客户标签等细粒度数据范围。
5. 页面不展示 API 路径、内部枚举、权限编码或数据库字段。
6. 关联用户区只提供跳转入口，不在角色页直接分配用户。

## 2. 自检记录

| 检查项 | 结果 |
|--------|------|
| HTML 解析 | `python3 -m html.parser docs/原型设计/16、邮件工单系统-角色配置管理原型.html` 通过 |
| 技术字段检索 | 未命中 API 路径、内部枚举、权限编码和数据库字段 |
| PNG 截图 | Chrome headless 生成 1440x2200 截图 |
| 视觉检查 | 左侧菜单、指标卡、角色列表、权限勾选、数据范围、生效预览和关联用户区未见重叠或截断 |

## 3. 待谭总确认

1. “角色管理”是否作为系统管理下的独立菜单。
2. 第一版是否只允许编辑自定义角色，内置管理员、客服处理人只读保护。
3. 用户分配角色是否继续留在用户管理，不在角色页直接分配。
4. 数据范围第一版是否只支持自己范围、全部范围、跟随工单范围，不做指定邮箱和客户标签。
