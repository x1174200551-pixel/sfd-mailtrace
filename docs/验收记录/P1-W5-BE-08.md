# P1-W5-BE-08 工作台统计验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-27 11:05 CST |
| 验收范围 | 工作台统计接口、工单统计口径复用、权限校验、接口冒烟 |
| 当前结论 | 后端 service/controller、单测和 HTTP 冒烟通过 |

## 1. 实现范围

| 项目 | 结果 | 说明 |
|------|------|------|
| 接口 | ✅ | 新增 `GET /api/v1/dashboard/summary` |
| Service | ✅ | 新增 `DashboardService`，查询工作台核心统计 |
| VO | ✅ | 新增 `DashboardSummaryVO` |
| 统计口径 | ✅ | `totalCount` 改为显式 `is_deleted=0`，与工单统计口径一致 |
| 权限 | ✅ | 管理员和处理人可访问，其他角色/未登录拒绝 |
| Service 步骤 | ✅ | 权限、状态统计、活跃数汇总均补充业务步骤注释 |

## 2. 返回指标

| 字段 | 口径 |
|------|------|
| `totalCount` | 未逻辑删除工单总数 |
| `pendingAssignCount` | `status=PENDING_ASSIGN` |
| `processingCount` | `status=PROCESSING` |
| `waitingCustomerCount` | `status=WAITING_CUSTOMER` |
| `slaOverdueCount` | `sla_breached=1` |
| `closedTodayCount` | 今日关闭且 `status=CLOSED` |
| `activeCount` | 待分配 + 处理中 + 待客户回复 |

## 3. 验证证据

| 命令/动作 | 结果 |
|------|------|
| `cd backend && mvn -q -Dtest=DashboardServiceTest,TicketBizServiceTest test` | 通过 |
| `cd backend && mvn -q test` | 通过 |
| 后端重启 `cd backend && mvn spring-boot:run` | 通过 |
| HTTP 冒烟：登录 -> `/api/v1/dashboard/summary` -> `/api/v1/tickets/stats` 对齐 | 通过 |

## 4. 冒烟样例

| 字段 | 值 |
|------|------|
| `totalCount` | `10` |
| `pendingAssignCount` | `0` |
| `processingCount` | `5` |
| `waitingCustomerCount` | `2` |
| `slaOverdueCount` | `0` |
| `closedTodayCount` | `0` |
| `activeCount` | `7` |
| 对齐结果 | 与 `/api/v1/tickets/stats` 核心字段一致 |

## 5. 后续

下一步进入 `P1-W5-BE-09 我的待办`，实现 `/api/v1/dashboard/my-todos` 并按当前用户过滤。
