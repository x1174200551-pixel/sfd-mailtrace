# G4 工单处理门验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-27 09:50-09:58 CST |
| 验收范围 | 工单回复、首次响应、内部备注、转派、优先级、关闭、客户追信、非法状态 |
| 当前结论 | 自动化回归与本地现场验收通过 |

## 1. 自动化证据

| 命令 | 结果 | 说明 |
|------|------|------|
| `cd backend && mvn -q -Dtest=TicketBizServiceTest test` | 通过 | 覆盖 G4 工单处理核心服务行为 |
| `cd backend && mvn -q test` | 通过 | 后端全量测试通过 |
| `cd frontend && pnpm build` | 通过 | 前端生产构建通过；仅 Vite chunk size 提醒 |

## 2. 现场证据

| 项目 | 结果 | 证据 |
|------|------|------|
| 后台回复客户 | ✅ | `TCK-20260724-004` 发送唯一标记 `G4-LIVE-20260727-0953`；`mt_mail_send_log.id=36` 为 `AGENT_REPLY / SUCCESS / to=x18772865262@163.com` |
| 收信确认 | ✅ | 定时拉信 `mt_mail_fetch_log.id=351` 成功拉取 2 封；`mt_ticket_message.id=22` 为 `INBOUND`，正文包含 `G4-LIVE-20260727-0953` |
| 转派同步 | ✅ | `TCK-20260724-005` 转回 `验收客服已编辑`；接口响应、列表查询、详情查询的 `assigneeId=2` 一致 |
| 页面详情确认 | ✅ | 浏览器打开前端详情页，`工单信息 / 负责人` 显示 `验收客服已编辑` |
| 通知开关复验 | ✅ | 使用最新后端传 `notifyAssignee:false` 后，未新增 `ASSIGN_NOTIFY` 发送日志 |

> 说明：本轮真实收信为了可闭环验证，使用了已配置的 163 邮箱自发自收，因此系统会把该入站邮件按新客户来信处理并生成测试工单；这属于验收方式带来的测试数据，不影响真实客户邮箱场景。

## 3. 用例结果

| 编号 | 用例 | 结果 | 证据 |
|------|------|------|------|
| G4-01 | 后台回复客户 | ✅ | 单测验证回复链路；现场验证 SMTP 成功日志与 IMAP 收信入站消息 |
| G4-02 | 首次回复 | ✅ | 对外首次回复时写 `first_reply_at` 并写 `FIRST_REPLY` 事件 |
| G4-03 | 内部备注 | ✅ | 内部备注不调用 `MailSendService`，不写 `first_reply_at`，仅写 `INTERNAL_NOTE` |
| G4-04 | 转派 | ✅ | 单测验证转派原因、通知开关和事件；现场验证列表/详情 API 与页面详情负责人同步 |
| G4-05 | 修改优先级 | ✅ | `updatePriority_shouldWriteChinesePriorityAndReason` 验证优先级事件中文展示和变更说明 |
| G4-06 | 关闭 | ✅ | `closeTicket_shouldWriteSingleClosedEventWithChineseStatus` 验证 `closed_at` 写入、关闭事件合并为单条、状态中文展示 |
| G4-07 | 关闭后客户追信 | ✅ | `handleCustomerFollowUp_whenClosed_shouldReopenToProcessing` 验证关闭工单追信后重开并写 `REOPENED`；`handleCustomerFollowUp_whenWaitingCustomer_shouldReturnToProcessing` 验证待客户回复回流处理中 |
| G4-08 | 非法状态 | ✅ | `updateStatus_whenIllegalWaitingCustomer_shouldReject` 验证接口拒绝手动变更为 `WAITING_CUSTOMER` |

## 4. 本轮修复

1. 关闭/取消事件保持单条，不再额外生成重复 `STATUS_CHANGED`。
2. 状态、优先级事件展示中文标签，避免前端直接暴露枚举。
3. 客户追信重开事件中的原状态改为中文显示。
4. 新增 `TicketBizServiceTest`，覆盖 G4 工单处理服务层关键路径。
