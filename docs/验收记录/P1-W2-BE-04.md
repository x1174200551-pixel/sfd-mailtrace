# P1-W2-BE-04 IMAP 拉信验收记录

| 项目 | 结论 |
|------|------|
| 工作包 | `P1-W2-BE-04` |
| 当前状态 | ✅ 已完成 |
| 日期 | 2026-07-23 |
| 前置 | 邮箱配置 BE-01/02/03 ✅；C02 契约 ✅ |

## 实施范围

| 模块 | 结果 |
|------|------|
| Flyway V5 | 为 `mt_mail_fetch_log` 增加 `trigger_type`（SCHEDULED/MANUAL） |
| 实体/Mapper | `MailFetchLogEntity`、`MailFetchLogMapper` |
| IMAP 客户端 | `ImapFetchClient` + `JakartaMailImapFetchClient`（统计未读数） |
| 业务服务 | `MailFetchBizService`：到期判断、单邮箱拉取、成功/失败写日志、回写 `lastFetchAt`/`connectionStatus` |
| 定时任务 | `MailFetchJob`，默认每 60 秒扫描到期启用邮箱 |
| 建单解析 | 本任务不做（留给 `P1-W2-BE-05`）；`createdTicketCount`/`linkedCount` 暂记 0 |

## 自测证据

| 验证项 | 命令/方式 | 结果 |
|--------|-----------|------|
| 单元测试 | `mvn -q test` | ✅ 5 个用例通过（到期判断、成功写日志、失败写错误） |
| Flyway | 启动应用迁移 | ✅ `Migrating schema mailtrace to version "5 - mail fetch log trigger type"` |
| 范围裁剪 | 代码检索 | ✅ 本阶段不解析邮件、不创建工单 |

## 说明

- 手动重试入口由后续拉取日志接口 `POST /api/v1/mail-fetch-logs/{id}/retry` 调用 `fetchMailbox(..., MANUAL)`。
- 真实企业邮箱联调依赖已配置并启用的 IMAP 账号；无账号时定时任务仍会按间隔尝试并写入失败日志。

## 下一步

进入 `P1-W2-BE-05` 邮件解析，或并行推进拉取日志查询接口 + `P1-W2-FE-02`。
