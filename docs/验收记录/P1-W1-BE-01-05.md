# P1-W1-BE-01~05 登录后端闭环验收记录

| 项目 | 结论 |
|------|------|
| 任务编号 | P1-W1-BE-01~05 |
| 当前状态 | ✅ 已完成 |
| 验收时间 | 2026-07-22 16:27 CST |
| 验收人 | AI |
| 启动方式 | 按 `backend/src/main/resources/application.yml` 连接 Docker MySQL |

## 覆盖范围

- `P1-W1-BE-01`：`V2__init_data.sql` 初始化管理员、默认参数、自动回执模板。
- `P1-W1-BE-02`：新增 `UserEntity`、`UserMapper` 与登录业务服务。
- `P1-W1-BE-03`：实现 `POST /api/v1/auth/login`。
- `P1-W1-BE-04`：实现 `GET /api/v1/auth/me`。
- `P1-W1-BE-05`：接入 JWT 生成、解析与 Spring Security 过滤。

## 验证命令

```bash
mvn -q -DskipTests compile
mvn -q test
mvn -q spring-boot:run
```

## 接口冒烟结果

| 用例 | 结果 |
|------|------|
| Flyway V2 | 成功迁移到 version 2 |
| 健康检查 | `200 / code=0` |
| 错误密码 | `401 / code=40101` |
| 管理员登录 | `200 / code=0`，返回 Bearer token |
| 当前用户 | `200 / code=0`，返回 `admin / 系统管理员 / ADMIN` |
| 未登录访问 `/me` | `401 / code=40102` |

## 备注

- 首次启动时发现模板内容 `${ticketNo}` 被 Flyway 识别为占位符，已按数据库设计基线改为 `{ticket_no}`。
- MySQL 8 对 `VALUES(col)` 有弃用警告，但迁移执行成功；后续如升级 MySQL 严格策略，可单独优化迁移写法。
