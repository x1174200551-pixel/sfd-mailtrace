# P1-W5-BE-11 报表埋点核查验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-27 11:18 CST |
| 验收范围 | created、first_reply、closed、sla、assignee、mailbox 报表字段完整性 |
| 当前结论 | 字段、写入链路、对外返回和测试证据通过；未新增报表接口 |

## 1. 核查范围

| 埋点字段 | 存储位置 | 写入/来源 | 对外返回 | 结论 |
|------|------|------|------|------|
| created | `mt_ticket.created_at` | 工单入库数据库默认值 | `TicketVO.createdAt`、`TicketSummaryVO.createdAt` | ✅ |
| first_reply | `mt_ticket.first_reply_at` | 处理人首次对外回复写入；内部备注不写 | `TicketVO.firstReplyAt`、列表 `hasReplied` | ✅ |
| closed | `mt_ticket.closed_at` | 关闭工单写入 | `TicketVO.closedAt` | ✅ |
| sla | `sla_policy_id`、`sla_response_deadline`、`sla_resolve_deadline`、`sla_breached`、`sla_warning_sent`、`sla_breach_notified` | 建单计算截止时间；SLA 检查任务写 warning/breach | `TicketVO`、`TicketSummaryVO`、工作台统计 | ✅ |
| assignee | `mt_ticket.assignee_id` | 分配规则、邮箱默认处理人、手动转派写入 | `assigneeId`、`assigneeName` | ✅ |
| mailbox | `mt_ticket.mailbox_id` | 拉信建单来源邮箱写入 | `mailboxId`、`mailboxName` | ✅ |

## 2. 代码核查

| 文件 | 核查结果 |
|------|------|
| `backend/src/main/resources/db/migration/V1__baseline.sql` | `mt_ticket` 已包含报表依赖字段和索引 |
| `backend/src/main/java/com/ntn/fziot/mailtrace/repox/mysql/entity/TicketEntity.java` | Entity 已映射 created、first_reply、closed、sla、assignee、mailbox 字段 |
| `backend/src/main/java/com/ntn/fziot/mailtrace/application/bizservice/ticket/TicketBizService.java` | 建单、分配、回复、关闭、列表筛选和详情返回链路完整 |
| `backend/src/main/java/com/ntn/fziot/mailtrace/application/bizservice/sla/SlaCheckService.java` | warning/breach 幂等写入，事件记录完整 |
| `backend/src/main/java/com/ntn/fziot/mailtrace/application/bizservice/dashboard/DashboardService.java` | 工作台统计和我的待办已使用 SLA、处理人、邮箱、创建时间字段 |
| `backend/src/main/java/com/ntn/fziot/mailtrace/interfaces/vo/ticket/TicketVO.java` | 详情接口返回报表核心字段 |
| `backend/src/main/java/com/ntn/fziot/mailtrace/interfaces/vo/ticket/TicketSummaryVO.java` | 列表接口返回处理人、邮箱、SLA、创建时间和首次响应状态 |

## 3. 本次补强

| 项目 | 结果 |
|------|------|
| 测试补强 | `TicketBizServiceTest.createTicket_shouldApplySlaDeadlines` 增加 status、mailbox、customer、SLA 初始标识断言 |
| Service 步骤 | ✅ 相关业务 service 已有步骤注释；本任务未新增 service |

## 4. 验证证据

| 命令/动作 | 结果 |
|------|------|
| `cd backend && mvn -q -Dtest=TicketBizServiceTest test` | 通过 |
| `cd backend && mvn -q test` | 通过 |
| HTTP 冒烟：登录 -> `/api/v1/tickets?page=1&size=1` -> `/api/v1/tickets/{id}` | 通过 |

## 5. 冒烟样例

| 字段 | 值 |
|------|------|
| 登录账号 | `admin` |
| 样例工单 ID | `10` |
| `mailboxId` / `mailboxName` | `3` / `txx测试邮箱` |
| `assigneeId` / `assigneeName` | `2` / `验收客服已编辑` |
| `firstReplyAt` | `null` |
| `closedAt` | `null` |
| `slaResponseDeadline` | `null` |
| `slaResolveDeadline` | `null` |
| `slaBreached` | `false` |
| `createdAt` | `2026-07-27T01:54:52.585` |
| 结论 | 报表核心字段已通过接口返回；空值符合当前样例工单数据状态 |

## 6. 后续

下一步进入 `P1-W0-08 分配规则原型`。`P1-W5-FE-01` 需要等待 `PG-08` 和 `P1-W0-C04` 通过后才能开始前端实施。
