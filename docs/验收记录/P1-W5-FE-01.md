# P1-W5-FE-01 分配规则前端验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-27 11:37 CST |
| 验收范围 | 分配规则前端页面、真实接口接入、构建、接口冒烟、前端服务 |
| 当前结论 | 分配规则页面已按 PG-08 和接口契约实施，前端构建通过 |

## 1. 实现范围

| 项目 | 结果 | 说明 |
|------|------|------|
| 菜单入口 | ✅ | SLA 管理下新增「分配规则」 |
| 列表 | ✅ | 接入 `GET /api/v1/assignment-rules`，支持关键词、状态、匹配类型筛选 |
| 摘要 | ✅ | 展示规则总数、默认兜底、测试命中、未保存修改 |
| 编辑 | ✅ | 支持规则名、匹配类型、匹配值、优先级、处理人、启用状态、分配通知 |
| 新建 | ✅ | 新建草稿并保存到 `POST /api/v1/assignment-rules` |
| 启停 | ✅ | 接入 `PATCH /api/v1/assignment-rules/{id}/enabled` |
| 排序 | ✅ | 上移/下移后调用 `PUT /api/v1/assignment-rules/sort` |
| 测试匹配 | ✅ | 接入 `POST /api/v1/assignment-rules/test-match` |
| 删除确认 | ✅ | 删除前二次确认，接入 `DELETE /api/v1/assignment-rules/{id}` |
| 四态 | ✅ | 覆盖加载、空列表、接口失败、无权限 |
| 原型一致性 | ✅ | 按 `docs/原型设计/11、邮件工单系统-分配规则原型.html/png` 的页面范围实施 |

## 2. 验证证据

| 命令/动作 | 结果 |
|------|------|
| `cd frontend && pnpm build` | 通过 |
| `cd frontend && pnpm lint` | 通过；保留既有 hooks 依赖 warning |
| `cd backend && mvn -q test` | 通过 |
| 后端接口冒烟：登录 -> 分配规则列表 -> 测试匹配 | 通过 |
| 前端 dev server：`cd frontend && pnpm dev --host 127.0.0.1` | 通过 |
| `curl -I http://127.0.0.1:5173/` | 200 OK |

## 3. 冒烟样例

| 字段 | 值 |
|------|------|
| 登录账号 | `admin` |
| 分配规则列表 | 当前环境 `records=[]`、`summary.totalCount=0` |
| 测试匹配 | 当前环境 `matched=false`，符合无启用规则数据状态 |
| 前端地址 | `http://127.0.0.1:5173/` |

## 4. 后续

下一步进入 `P1-W0-09 SLA 策略原型`。SLA 前端页面仍需先过 PG-09，再补对应接口契约切片后实施。
