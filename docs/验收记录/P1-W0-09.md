# P1-W0-09 / PG-09 SLA 策略原型验收记录

| 项目 | 说明 |
|------|------|
| 工作包 | `P1-W0-09` SLA 策略原型 |
| Gate | `PG-09` |
| 状态 | 🧪 待谭总确认 |
| 自检时间 | 2026-07-27 11:54 CST |
| 原型 HTML | `docs/原型设计/12、邮件工单系统-SLA策略原型.html` |
| 原型 PNG | `docs/原型设计/12、邮件工单系统-SLA策略原型.png` |

## 1. 范围

本次只补齐 SLA 策略页面原型，不进入 SLA 策略前端实现，不制作工作日历/节假日页面原型。

原型覆盖：

- SLA 策略列表、关键字筛选、启停筛选、默认策略筛选。
- 新建/编辑策略：策略名称、启用策略、默认策略、首次响应时限、解决时限、预警阈值、升级阈值、绑定日历。
- 默认策略约束：默认策略必须启用；设置默认后其他策略取消默认。
- 绑定工作日历：展示时区、工作日、工作时段、节假日跳过口径。
- SLA 截止时间预览：建单时间、首次响应截止、解决截止、预警和超时升级时间线。
- 启停、设置默认、删除确认、保存失败保留草稿。
- 空列表、加载中、接口失败、无权限四态。

## 2. 对齐依据

- 现有原型风格：`docs/原型设计/11、邮件工单系统-分配规则原型.html`。
- 后端 SLA 策略字段：`policyName`、`enabled`、`defaultPolicy`、`responseHours`、`resolveHours`、`warningRemainHours`、`escalateAfterBreachHours`、`calendarId`。
- 后端接口入口：`GET/POST/PUT/PATCH/DELETE /api/v1/sla-policies`。
- 工作日历绑定入口：`GET /api/v1/work-calendars`，用于后续页面接口契约补充。

## 3. 自检命令

```bash
python3 -m html.parser 'docs/原型设计/12、邮件工单系统-SLA策略原型.html'
```

结果：通过，无 HTML 解析错误。

```bash
rg -n "新建策略|响应时限|解决时限|预警阈值|升级阈值|绑定日历|默认策略|启用策略|停用|删除确认|空列表|加载中|接口失败|无权限|工作日历|重新计算" 'docs/原型设计/12、邮件工单系统-SLA策略原型.html'
```

结果：通过，关键控件和四态均已覆盖。

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless \
  --disable-gpu \
  --no-first-run \
  --no-default-browser-check \
  --window-size=1440,1024 \
  --screenshot="/Users/tanzhixing/workSpace/sfd-mailtrace/docs/原型设计/12、邮件工单系统-SLA策略原型.png" \
  "file:///Users/tanzhixing/workSpace/sfd-mailtrace/docs/原型设计/12、邮件工单系统-SLA策略原型.html"
```

结果：通过，已生成 1440x1024 PNG 快照。

## 4. 结论

AI 自检通过，`PG-09` 标记为 🧪 待谭总确认。确认前不得进入 `P1-W5-FE-02 SLA 策略` 前端开发。
