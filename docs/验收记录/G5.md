# G5 P1 核心业务总门验收记录

| 字段 | 内容 |
|------|------|
| 验收时间 | 2026-07-28 12:00-14:25 CST |
| 验收范围 | 建单、分配、SLA、工作台、客户只读、权限、G1-G4 回归 |
| 当前结论 | 技术验收通过，待谭总业务确认 |

## 1. 自动化证据

| 命令 | 结果 | 说明 |
|------|------|------|
| `cd backend && mvn -q -Dtest=TicketBizServiceTest test` | 通过 | 覆盖建单、回复、转派、关闭、客户追信、邮箱默认处理人兜底 |
| `cd backend && mvn -q -Dtest=SlaCheckServiceTest test` | 通过 | 覆盖 SLA 即将超时、已超时、幂等跳过和提醒邮件发送 |
| `cd backend && mvn -q test` | 通过 | 后端全量回归通过 |
| `cd frontend && pnpm build` | 通过 | 前端生产构建通过；仅 Vite chunk size 提醒 |
| `cd frontend && pnpm lint` | 通过 | 仅保留既有 `fetchFetchLogStats` / `fetchSendLogStats` hooks 依赖 warning |
| `git diff --check` | 通过 | 未发现空白或冲突标记问题 |

## 2. 接口与现场证据

| 项目 | 结果 | 证据 |
|------|------|------|
| 后端启动 | ✅ | `mvn spring-boot:run` 启动成功，Tomcat 8080，context path `/api`；Flyway validated 9 migrations，schema version 9，无待迁移 |
| 登录与统计 | ✅ | `POST /api/v1/auth/login` 成功；工单统计 `total=10, processing=5, waitingCustomer=2, slaOverdue=0, active=7` |
| SLA 默认策略 | ✅ | `GET /api/v1/sla-policies` 返回 1 个默认策略：`2026日常业务处理2小时响应8小时处理策略` |
| 默认工作日历 | ✅ | `GET /api/v1/work-calendars` 返回 1 个默认日历：`客服工作日历` |
| 分配规则命中 | ✅ | `POST /api/v1/assignment-rules/test-match`：VIP 主题命中 `VIP客户` 规则，普通主题命中默认规则 |
| 角色权限 | ✅ | 使用 AGENT 角色 token 冒烟：`/users`、`/assignment-rules`、`/sla-policies`、`/work-calendars` 均 403；`/tickets` 可访问 |
| 工作台跳转 | ✅ | 浏览器点击 `处理中工单` 后进入全部工单并筛出 5 条，标题显示 `当前筛选共 5 个工单`，分页 `共 5 条` |
| SLA 超时入口 | ✅ | 浏览器点击工作台 SLA 超时风险卡片后进入全部工单，筛选结果 0 条，空列表状态正常 |
| 客户只读页 | ✅ | 客户列表展示 4 位客户；无新建/编辑客户按钮；详情备注只读 |
| 客户关联工单 | ✅ | 客户详情点击 `TCK-20260727-002` 可进入工单详情，展示工单号、客户邮箱、邮件会话和生命周期 |

## 3. 用例结果

| 编号 | 用例 | 结果 | 证据 |
|------|------|------|------|
| G5-01 | 主闭环 | 🧪 技术通过，待谭总确认 | G3/G4 已完成真实建单、回执、通知、回复、关闭现场验收；本轮做全量回归和页面联动复验，未重复制造新业务邮件 |
| G5-02 | 关键词分配 | ✅ | 现场 `test-match` 验证 VIP 主题命中关键词规则；`AssignmentRuleServiceTest` 覆盖优先级、停用规则、处理人有效性 |
| G5-03 | 来源邮箱分配 | ✅ | `TicketBizServiceTest.createTicket_whenNoRuleMatches_shouldFallbackMailboxDefaultAssignee` 断言保存工单负责人取当前邮箱默认处理人；当前本地仅 1 个启用邮箱，双邮箱现场需补第二邮箱样本 |
| G5-04 | 非工作时间来信 | ✅ | `SlaDeadlineServiceTest.calculateForNewTicket_whenAfterWorkTimeAndHoliday_shouldSkipToNextWorkingDay` 验证非工作时间和节假日顺延 |
| G5-05 | 即将超时提醒 | ✅ | `SlaCheckServiceTest` 验证到阈值只标记一次并发送 `SLA_WARNING` 邮件 |
| G5-06 | 已超时提醒 | ✅ | `SlaCheckServiceTest` 验证写 `sla_breached`、事件和 `SLA_BREACH` 邮件；重复扫描不重复发送 |
| G5-07 | 工作台数字 | 🧪 技术通过，待谭总确认 | 接口统计与页面跳转筛选一致：处理中 5，SLA 超时 0，活跃 7 |
| G5-08 | 客户页 | 🧪 技术通过，待谭总确认 | 浏览器复验客户页只读、无新建/编辑按钮、关联工单跳详情 |
| G5-09 | 角色权限 | ✅ | AGENT token 对管理功能返回 403，对工单列表返回 200 |
| G5-10 | G1-G4 回归 | ✅ | 后端全量测试、前端构建、前端 lint、接口冒烟、浏览器联动均通过 |

## 4. 本轮修复

1. 新增 `V9__backfill_default_sla_policy_calendar.sql`，修复本地旧数据中启用 SLA 策略没有默认项的问题，并同步兜底默认工作日历。
2. `SlaCheckService` 补齐 SLA 即将超时和已超时邮件提醒：优先使用通知模板，模板缺失时使用系统兜底内容，发送失败不回滚 SLA 状态。
3. `TicketBizServiceTest` 增强邮箱默认处理人兜底断言，覆盖 G5 来源邮箱分配口径。
4. 全部工单页面标题改为当前筛选口径，避免从工作台卡片跳转后标题仍显示全量统计。
5. 左侧菜单隐藏未实现占位入口：我的工单、待处理、待客户回复、已关闭、超时记录、统计报表、操作日志；全部工单页隐藏未确认的手工新建工单入口。

## 5. 待谭总确认点

1. G5-01 主闭环是否接受复用 G3/G4 真实现场证据，不再重复发送新业务邮件。
2. G5-07 工作台数字和跳转口径是否符合业务预期。
3. G5-08 客户页第一版只读范围是否确认进入上线硬化。
