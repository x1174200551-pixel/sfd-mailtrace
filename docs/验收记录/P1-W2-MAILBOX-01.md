# P1-W2-MAILBOX-01 邮箱管理开发验收记录

## 基本信息

| 项 | 内容 |
|----|------|
| 工作包 | `P1-W2-BE-01/02/03 + P1-W2-FE-01` |
| 页面原型 | PG-05 邮箱管理中心 |
| 接口契约 | `docs/接口契约/P1-W0-C02.md` |
| 状态 | 🧪 待谭总验收 |
| 日期 | 2026-07-23 |

## 实施范围

| 模块 | 结果 |
|------|------|
| 邮箱配置接口 | 已实现列表、新增、编辑、启停、删除 |
| 密码存储 | IMAP/SMTP 密码或授权码加密保存；响应不返回明文 |
| 连接测试 | 支持已保存邮箱和草稿配置测试；支持 `ALL`、`IMAP`、`SMTP` |
| 前端页面 | 已接入 AppShell 的“邮箱配置”菜单，完成统计、筛选、列表、分页、空态、编辑面板、测试、保存和确认弹窗 |
| 范围限制 | 一期仅支持 IMAP 收信；不做 POP3；不做真实拉信 Job；不做收件记录/发件记录独立页 |

## 自测证据

| 验证项 | 命令/方式 | 结果 |
|--------|-----------|------|
| 后端测试 | `mvn -q test` | ✅ 通过 |
| 前端构建 | `npm run build` | ✅ 通过 |
| 前端 lint | `npm run lint` | ✅ 通过 |
| 登录接口 | `POST /api/v1/auth/login`，`admin/admin123` | ✅ 返回 token |
| 邮箱列表接口 | `GET /api/v1/mailboxes?page=1&size=10`，携带 Bearer Token | ✅ `200 / code=0` |
| 页面烟测 | Chrome 自动登录后点击“邮箱配置” | ✅ 进入页面，无未登录弹出 |
| 页面请求头 | Playwright 监听 `/api/v1/mailboxes` | ✅ status `200` |
| 页面错误 | Playwright `pageerror` 监听 | ✅ 无页面错误 |
| 操作条可见性 | 1440x900 视口检查保存按钮坐标 | ✅ 保存按钮在视口内 |
| 反馈修复 | Chrome 点击邮箱列表第二行 | ✅ 行可选中；右侧编辑区同步选中邮箱；编辑区收窄、列表区加宽 |
| 默认处理人 | Chrome 打开邮箱页并监听 `/api/v1/users` | ✅ 字段由 ID 输入改为处理人下拉；留空表示按分配规则处理 |
| 新增邮箱表单 | Chrome 新增邮箱并滚动编辑区到底部 | ✅ placeholder 降权；回执模板不再填 ID；底部操作条不遮挡字段 |

## 待谭总验收

1. 检查页面是否符合 PG-05 原型风格和业务字段口径。
2. 检查新增、保存、测试收信、测试发信、测试全部、启停、删除确认是否符合操作习惯。
3. 如验收通过，下一步进入 `P1-W0-06 拉取日志原型`。
